# Screenshots

Place the following submission screenshots in this folder and they will render
from the root `README.md`:

| File | What to capture |
| --- | --- |
| `mobile.png` | The live demo in a mobile viewport (DevTools device toolbar) |
| `ci.png` | The GitHub Actions CI run with all jobs green |
| `tests.png` | Terminal output of `cargo test` and `npm test` (all passing) |
